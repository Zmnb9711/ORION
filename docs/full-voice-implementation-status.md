# First full voice milestone — incomplete, provider semantic gate blocked

**Historical Qwen-gate snapshot, superseded for the bounded ownship field path.**
The user subsequently authorized the deterministic semantic middle. The original
Qwen failure evidence below is retained unchanged; it is not the selected field
runtime. See `full-voice-field-ready.md` for the current implementation, tests and
successful live Core/protected TTS gate. Qwen/Core provenance checks remain intact.

Architecture Guard: OFF, historical recovery exemption.

Baseline HEAD remains `9362f18bd320da8e92daf6b680d68cfd08c6b8ad` on
`recovery/a955d7c-radio-validated`. No implementation commit or push has been
made. The working changes below are an uncommitted implementation, not a field-
validated release. Do not treat field-host startup READY as a completed voice gate.

## Provider/billing versus Core handoff

The current account successfully executed actual requests against
`https://ai.api.cloud.yandex.net/v1/responses`, model `qwen3.6-35b-a3b`.
Observed HTTP status: 200; a native `PlannerToolCallsEvent` was decoded, with
provider diagnostic failure_category absent. Authentication, model access,
billing or quota did not block those requests. Account balance and general quota
headroom were not independently queried; no claim of unlimited availability is
made. Credentials, headers and raw provider bodies were not logged.

The original handoff failed at `ToolGateway.execute`: the model supplied extra
fields to `NoArguments`; Core rejected `invalid_arguments` before handler start.
The stricter schema's `extra_forbidden` categories were observed. A direct call
to the same actual live gateway succeeded with all three requested scalar leaves
KNOWN and AUTHORITATIVE. This distinguishes provider availability from input-
contract compliance; the gateway failure was not a disguised billing failure.

A field-only adapter now represents NoArguments on the provider wire as exactly
`{"no_arguments": true}` and decodes only that representation to Core `{}`.
No other argument content is silently discarded; original IA-5/IA-3 definitions,
permissions, handlers and validation remain unchanged. The live tool call then
completed. Further provider continuation failures remain:

- `invalid_final_response`: all three requested values/units matched retained
  ToolResults, but all three source reference IDs did not match the completed
  call. Core correctly rejected them.
- `tool_round_limit_exceeded`: provider requested another tool round despite the
  existing IA-6 single-round policy. The field wire adapter now removes tool
  exposure after completion and requests final semantics only.
- A final attempt additionally constrained source.reference_id to the actual
  completed call in the output schema and instructions; it reached the bounded
  Core deadline without an accepted final SemanticResponse.

The `[200, 598, 598]` diagnostic sequence in the last report includes locally
normalized timeout statuses: 598 is generated by the historical transport retry
adapter, NOT evidence of an actual Yandex HTTP 598 response or billing rejection.
No provenance repair, fact replacement, validation weakening, new model, or
deterministic bypass of IA-6 has been introduced.

The unresolved blocker is an accepted real provider continuation with exact
Core-owned source references under the existing semantic contract. Offline
success is not sufficient to close it. No full user-spoken field gate was run.

## Implemented working changes

- `full_voice_capture.py`: one physical owner; local UDP7082 start/end, bounded
  candidate skew and drain, no packet-gap/VAD EOU, busy/mismatch/overflow rejection.
- `full_voice_stt.py` / `speechkit_v3_stt_transport.py`: bounded native streaming
  RPC; immutable FinalizedUserUtterance only after matching FINAL/EOU_UPDATE.
- `full_voice_live_world.py`: exclusive real UDP45100 ingress, existing
  TelemetryProtocol, shared live_telemetry and world_model; no competing owner.
- `full_voice_core.py`: actual InteractionRouter/Planner/ToolGateway, retained
  ToolResults, exact source/value binding, renderer and ResponseComposer.
- `ownship_report.py` / `ownship_phraseology.py`: separately versioned non-
  normative declarative ownship report, lossless numeric scalar spelling and
  provenance. Existing PILOT_SYNTHETIC_V1 command strings are unchanged.
- `protected_streaming_tts.py` / `protected_streaming_presentation.py`: direct
  john synthesis of exact finalized text, no strip/translation/SSML/rewrite;
  inherits Stage 7C ingress, composition, replay and identity rules.
- `bounded_radio_stream.py`, radio contracts/router/adapter extensions: explicit
  optional streaming capability and bounded PCM, not fake finalized audio.
- `full_voice_srs.py`: reuse SRS packet/Opus/resampling/absolute 40 ms pacing;
  controlled expected origin, half-duplex, bounded underrun, correlated terminal.
- `full_voice_field.py`: dedicated field runner, bounded duration/turn count,
  safe diagnostics, per-turn Temp evidence and cleanup. It is not yet certified
  ready for the user-spoken field gate.
- `full_voice_qwen.py`: opt-in provider-wire compatibility draft described above;
  its complete live semantic gate is still failing.

START LIVE, Launcher, original Realtime adapter, STT VAD and general production
provider service were not migrated. Existing Stage 7A/7B/7C golden behavior was
retained; new ownship wording is an explicit extension. Existing SRS finalized-
PCM callers retain their default path. No radio protocol, RadioInfo, Opus codec,
physical PTT mapping, or aircraft control implementation was replaced.

## Runtime evidence

- Isolated official SRS physical PTT: radio 1, 251.000 AM, 4437 ms observed held
  interval. See `full-voice-readiness-evidence.md` for earlier inconclusive windows.
- Implemented native STT gate: one known Russian recording, 112824 bytes PCM16
  at 16000 Hz, one immutable utterance, EOU-to-barrier 313 ms, resources closed.
  Transcript SHA-256:
  `6220749b546b383afba5f11b7d4e8d35a96bb4aa70c935722c66ce866a7f1d9c`.
- john v3 compatibility (not full protected report): exact test sentence accepted,
  226938 bytes at 48000 Hz; first and only chunk at 2359 ms. Not a sub-second pass.
- Actual recovery live-owner readiness: new DCS telemetry, heading/position KNOWN.
- Dedicated field-host startup/shutdown, no PTT/STT audio/TX:
  `C:/Users/CD86~1/AppData/Local/Temp/orion-full-voice-_7p_hais/report.json`.
- Real Core/provider reports (all explicitly NOT physical field evidence):
  `orion-full-voice-provider-4cnlf9ab/report.json` (first final rejection),
  `orion-full-voice-provider-znoym8pt/report.json` and
  `orion-full-voice-provider-yselaxrq/report.json` (extra tool rounds),
  `orion-full-voice-provider-g8m2osgb/report.json` (exact values, wrong sources),
  `orion-full-voice-provider-a7pi1z3w/report.json` (deadline).
  These directories are under `C:/Users/CD86~1/AppData/Local/Temp`.

## Verification and preservation

Ruff, compileall, and Pyright (the existing CI target list plus new runtime
components) passed. Targeted existing radio/presentation/phraseology regressions
passed (144 tests at that point); new tests also cover native barriers, half-
duplex, source preservation, exact protected text, streaming cancellation,
underruns, single TX and provider-error classification. Final broad-suite results
are recorded below when complete.

Final offline verification on 2026-09-07:

- Full suite, without coverage: **1739 passed, 3 failed**, one existing
  Starlette/httpx deprecation warning, 22.43 seconds. The three failures are
  `tests/test_setup_wizard_model.py::test_manual_dcs_and_saved_games_advance_deterministically`,
  `::test_changing_dcs_invalidates_saved_games_and_ready_state`, and
  `::test_auto_detect_candidate_invalidates_previous_ready_state`. They observe
  the real machine's Saved Games path and advance to INTEGRATION rather than
  the tests' expected SAVED_GAMES state; the same failures occurred in the
  pre-integration broad run. No Launcher fix is included.
- Targeted full_voice, full_voice_srs, full_voice_qwen, srs_tx_state, and
  interaction_contracts: **87 passed**, one existing warning.
- The first broad run exposed a new exact consumer-allowlist failure. The
  IA-0 boundary test now explicitly lists only the two added Core consumers:
  `full_voice_core.py` (InteractionRequest construction) and `ownship_report.py`
  (SemanticResponse validation). No wildcard/exclusion or provider exception
  was added. The test passes.
- Ruff across `orion tests`, compileall, and Pyright over the existing CI
  targets plus all new runtime modules: PASS, zero Pyright errors/warnings.
- `git diff --check`: PASS (Git emits normal LF/CRLF conversion warnings).
- Coverage run executed the same 1739 passing and three failing tests, but
  aggregation failed with `Can't combine branch coverage data with statement
  data`. A retry with a distinct Temp COVERAGE_FILE still encountered that
  aggregation error. Coverage is **not established**; no percentage or coverage
  gate pass is claimed. Existing coverage artifacts were not manually deleted.

Tracked diff at handoff: nine files, 74 insertions, 15 deletions; this excludes
the new untracked implementation/test/document files listed above and below.
The index is empty. HEAD and tree remain respectively
`9362f18bd320da8e92daf6b680d68cfd08c6b8ad` and
`c6bba53a747fa667613d9bba460cefb711d5febf`.

New test/probe files are `tests/test_full_voice.py`,
`tests/test_full_voice_srs.py`, `tests/test_full_voice_qwen.py`,
`tests/test_srs_tx_state.py`, and `tests/full_voice_provider_gate.py`.
The provider gate is explicit CLI work, not a pytest live request.

Baseline broad run before integration changes: 1691 passed, 3 setup-wizard
failures caused by machine Saved Games discovery. That unrelated Launcher logic
has not been changed. Pre-existing pacing-probe files/docs and generated data are
not selected for a commit. `data/fa18c_value_profiles.json` remains SHA-256
`4975E243EE95FDF997CCC5ECC4537EAFF87947265C1C9A7F876ECF3CCEE15C1F`.

DCS and the official SRS Client are externally owned and were not terminated or
restarted by the assistant. The installed ORION runtime was closed by the user
before isolated checks. Recovery observers/field processes have exited; UDP7082
and UDP45100 were verified unbound afterward. No recorded audio was saved by the
new probes and no operational reply was sent during this implementation session.

## Reference classification and source locations

Only the explicitly authorized later reference commit
`42520a57b01cd314978bcb51bdf4bbc75b38c156` was used for later voice patterns:

- PORT: `orion/srs_tx_state.py` and `tests/test_srs_tx_state.py`, local
  CombinedRadioState parsing/listening and tests.
- PORT: `orion/yandex_speechkit_v3_proto/` message definitions and provenance.
- ADAPT: `orion/yandex_speechkit_stt.py::speechkit_session_options` and
  `GrpcSpeechKitStreamingPort` into `speechkit_v3_stt_transport.py`; recovery
  coordinator owns the bounded finalized-utterance barrier separately.
- REFERENCE: `orion/live_golden_conversation.py`,
  `orion/yandex_srs_live_core.py`, `orion/radio_streaming.py`, and
  `orion/yandex_speechkit_streaming_tts.py` for physical lifecycle and streaming
  patterns. No unrelated later authority path or text `.strip()` was imported.

Exact current failure boundaries: `orion/tool_gateway.py:206` validates tool
arguments before handler invocation; `orion/planner.py:546` resolves the returned
source ID against retained results, with value binding at line 554. Timeout
normalization is `orion/yandex_qwen_planner.py:568`. The opt-in compatibility
draft is confined to `orion/full_voice_qwen.py`; it does not repair final output.

## Ownership, evidence, latency and remaining field instructions

One physical turn UUID is retained through immutable STT output,
InteractionRequest, ToolResult receipt, composition and correlated radio TX.
`PhysicalRadioTurn` does not release ownership until downstream terminal;
`NativeSpeechKitTurns` rejects ambiguous/mismatched barriers. The field runner
binds exactly one expected SRS peer, rejects busy/unknown sources and self RX,
and retains half-duplex permission through TX. Unsupported requests produce no
operational transmission. Buffer, turn, provider and shutdown bounds are
explicit; cancellation aborts pending PCM and provider tasks. Offline evidence
is not proof of physical/acoustic exactly-once delivery.

The known-recording STT barrier measured 313 ms after external EOU; the real
Core/provider attempt with matching values but invalid source references took
9516 ms, and the later attempt exhausted the 15-second Core deadline. The
separate short john compatibility call produced its only chunk at 2359 ms.
These are separate component measurements, not additive end-to-end field
latency. No first-SRS-frame or physical-end-to-response-start measurement exists
for the full vertical. The blocker is semantic correctness, not a modest miss
of the one-second performance objective.

**Do not press PTT for this runner yet.** Real provider semantics have not passed;
there is no safely approved physical test command at this handoff. The intended
later field sequence remains: official SRS radio 1 at 251.000 AM, fresh DCS
telemetry owned exclusively by recovery, say the authorized ownship query,
repeat after a real state change, then the unsupported query. Startup READY
alone does not authorize those tests. No new general preflight or stage is
proposed. The current milestone remains incomplete pending a real, validated
provider SemanticResponse with exact completed ToolResult source references.
