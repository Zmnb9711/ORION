# Foundation Step 5 — integration, pre-field work in progress

ORION ARCHITECTURE GUARD: OFF. NOT COMMITTED / NOT BUILT / NOT FIELD PASSED.

Pre-commit completion: [deadline/referent gate PASS and decision A](2026-09-15-foundation-step5-deadline.md).
Final3486 offline passes/five baseline failures, RU/EN4/4 live referent results;
one candidate commit/build/install authorized. Earlier pending/stop statements
below retain their checkpoint meaning. No Foundation FIELD PASS.

Latest bounded continuation: [referent contract report](2026-09-15-foundation-step5-referent.md).
Explicit typed recency correction,3478 offline passes/five baseline failures;
new authorized live gate timed out before its first RU terminal. No retries,
EN not run; original referent root cause/live correction remain unproven.
Earlier stop boundaries below describe the preceding sessions, not fresh authority.

## A. Contract and source identity

CONTRACT READ: `ORION_NATURAL_LANGUAGE_FIRST_CONTRACT_V1`, canonical
`docs/architecture/ORION_NATURAL_LANGUAGE_ARCHITECTURE_CONTRACT.md`.
Worktree `C:/Users/Алексей/Documents/GitHub/ORION-level0-conversation`;
branch `codex/general-natural-language-ingress-tranche1`;
parent/current HEAD `31546f9f191b060571cba32998c8cf232b345506`;
starting tree `7ea6f05fb2b6ce5a8507d83400496cf30d907424`.
Steps 1–4 are ancestors. Initial tracked/index state clean; existing `data/`
preserved. Contract SHA-256 unchanged:
`de59126cb79efc010996f4f8357ab80b641109b7ed3cd60ccbc7856d53c2a128`.

Compliance: §§1–3/6/12–13 keep open input and one General AI operation, no added
user-language vocabulary. §§4–5/7 preserve selected Core/Gateway authority;
§8 completes existing typed Mixed. §9 extends the same explicit context owner
across routes, not hidden provider memory. §§10–11 preserve one-hop/coalesced
facts and separate latency debt. §§14–15 require actual host preservation and
future blind installed-host acceptance; §§17–20 preserve source/scope/contract.
No contract conflict established. No provider/domain/persistent-context expansion.

## B–C. Step 1–4 preservation and exact integration changes

Eight runtime files, no new runtime module:

| File / symbols | Proven gap and bounded change |
|---|---|
| `general_semantic_contracts.py`: Mixed, ContextExchange, provider_instructions | Existing Mixed was explicitly unimplemented in instructions. Declare its composition role and exact nested output shape; add requested-capability context metadata. No provider values or input phrases. |
| `general_semantic_core.py`: MixedPlan, execute, render_general, InteractionContext | Existing Mixed fell through to NOT_IMPLEMENTED. Bind its facts with the existing `_facts`, compose with the non-authoritative fragment, retain common admission/expiry. Context retains selected semantic needs and non-factual text, never a dynamic snapshot. |
| `general_semantic_voice.py`: run | Only Dialogue previously entered context before audio. All General plans now do so; delivery updates the same exchange and never asserts user_heard. Emit opt-in bounded context projection. |
| `full_voice_service.py`: answer only, plus source_hash import | Fast-path answers previously bypassed shared context. Lazily establish the same General owner before route selection and record already-admitted fast-path topics. Completed failed fast-path delivery no longer kills later turns; nonterminal failure and endpoint/SRS failure still stop. No START/STOP/worker ownership redesign. |
| `general_fact_presentation.py` | General factual output was always Russian. Add deterministic English presentation using the same units/precision and canonical values. Default Russian behavior preserved. No input language grammar or TTS change. |
| `yandex_warm_aircraft_interpreter.py`: `_interpret` | Existing wire-kind budget switch covered Dialogue, not Mixed. Mixed uses that same finite Dialogue budget and must validate as Mixed. No retry, second call or wire/ACK/isolation change. |
| `interaction_router.py`: admit_general | Same Mixed budget distinction at admission. Request/deadline/provenance/replay checks remain; current facts are read afterward with existing freshness. Other router paths unchanged. |
| `realtime_test_evidence.py`: record_conversation_slice | Existing opt-in recorder accepts bounded explicit Interaction Context, enabling cross-turn reconstruction. No secret/provider body/audio recording or new recorder. |

Historical references are the committed Step 1–4 reports, the 57a563a/333ca5e
golden fast-path line and scoped current field evidence. KEEP existing routes,
transport and presentation owners; ADAPT only their required context/composition
integration. Older test scope assertions now inspect their exact historical
commits; separate Step 5 AST/scope tests and complete-file hashes constrain this
change. No historical helper removed or arbitrary production file exempted.

## D–E. Response ownership and Mixed

Unresolved/partial turns still enter General once. A Mixed proposal has a typed
Dialogue fragment and typed FactRequest. Core consumes one grant and performs
one coalesced read, producing one MixedPlan and one finalized response. The
same GeneralPresentation owns one TTS/radio operation. Nested request/deadline
binding, final-text identity and single-use admission are checked. Missing/stale
facts remain truthful unavailable fragments; no raw snapshot/stringification.
Provider prose is non-authoritative and receives no telemetry. A prompt cannot
prove arbitrary generated prose is always correct; blind content review remains
mandatory. No second model judge or per-phrase filter was added.

Whole-turn golden fast paths remain mutually exclusive alternatives under the
same host turn owner, with their existing protected/informational plans and
delivery implementations. They are not falsely described as General AI calls.

## F–H. Context, unavailable and fact surface

Same two whole exchanges / 4096 UTF-8 bytes / 300-second context policy, with
mission/aircraft/test-session epoch reset. Fast-path facts, General facts, Mixed,
Dialogue, META, clarification and unavailable now contribute semantic context.
Only non-authoritative conversational prose is retained as reply. Facts retain
IDs/topics plus response hash for delivery correlation, not numeric current
truth; every new factual request rereads Core. Pending acceptance precedes audio;
failed/cancelled delivery does not erase meaning or become acoustic confirmation.

Fact surface stays `orion.facts.177a3435e57b3705`, ten exposed facts / 1171-byte
semantic catalog. All 233 Step 4 dispositions and fuel SEMANTICS_UNCERTAIN remain.
No registry, Export, source-quality or WorldModel change. Truthful unavailable
is not confused with provider unavailability. RU/EN deterministic output uses
the same source values and DDM .01-minute precision. Voice pronunciation in
English remains a physical acceptance item, not a fixture-certified result.

## I. Offline/static evidence

Actual FullVoiceService matrix: 17 sequential turns, RU/EN, either golden fast
path first, with and without failed first TTS. Real Core/WorldModel/Gateway,
warm owner/parser, presentation and RadioRouter; fake STT finals, provider wire,
TTS PCM and radio sink. Covers Dialogue/knowledge/facts/Mixed/follow-ups/META/
unavailable/clarification, provider failure then success, TTS failure then
success, one response per turn, current-value rereads and unrelated-data exclusion.
Additional negative Mixed authority/schema/cancellation/staleness, language,
budget, scope and test-mode privacy checks. Not physical or generated-quality proof.

Pre-provider complete regression: **3466 PASS / five baseline FAIL / one skip**,
87.04 seconds. Baseline failures: IA import inventory; three Saved Games wizard
expectations; stale Project Memory exact phrase. Same set as Step 4; no new
unexpected failures. Changed runtime Pyright zero errors; Ruff/compileall/diff
checks passed. XML: `C:/Users/Алексей/AppData/Local/Temp/orion-foundation-step5-final.xml`.
Post-schema-correction complete regression: **3467 PASS / the same five baseline
FAIL / one skip**, 87.394 seconds; XML
`C:/Users/Алексей/AppData/Local/Temp/orion-foundation-step5-corrected-schema.xml`.
Ruff and diff checks passed. Changed-runtime Pyright with the actual ORION venv
explicitly selected: zero errors/warnings. An initial invocation without that
interpreter reported unresolved pydantic imports; corrected tool configuration,
not source edits, resolved those diagnostics.

## J. Single permitted provider gate — first failure preserved

Evidence:
`C:/Users/Алексей/AppData/Local/Temp/orion-foundation-step5-provider-once-20260915/report.json`.
One connection, one operation, zero retries, zero owned tasks after shutdown.
The planned four-operation developer-only RU/EN gate stopped on operation one.
No real DCS, TTS, SRS, audio or personal context was supplied.

IDs: interaction `2a06abc4-2151-4081-91f5-9fa8648e50e9`;
operation `b2535303-122a-4d80-8805-46e827ec6587`;
provider response `resp_b5dab6784c6a48cdbdd58446db54d1d7`.
Context ACK succeeded. First text796ms, terminal1156ms; cold1485ms separate.
RAW/normalized terminal selected MIXED and ownship.pitch, with a short generated
explanation, but omitted `facts.kind` and represented `dialogue` as a string.
Exact first validation failures: `missing` at `MIXED.facts.kind`; `model_type`
at `MIXED.dialogue`. No Core read, no TTS or TX. Not billing/auth evidence and
not a failure to select the intended current fact. Parsed proposal does not exist.

Bounded correction: replace abbreviated nested-schema prompt notation with the
literal required object structure, explicitly requiring both nested kind fields.
Strict validation still rejects the captured malformed shape; no permissive
normalization, text rewriting, partial execution or retry. Offline contract tests
cover the exact failure class and canonical accepted structure. Actual provider
compliance AFTER this clarification is recorded below. The original failed
report is not overwritten or relabelled PASS.

The user explicitly authorized one additional bounded session, without individual
request retries. It was performed once and is now exhausted.
Latency estimate remains roughly3–4sec to first voice from prior semantic/TTS
evidence, not a new physical measurement or <1sec claim.

### Additional authorized session — schema passes, referential selection fails

Evidence (original first failure remains separate):
`C:/Users/Алексей/AppData/Local/Temp/orion-foundation-step5-provider-corrected-20260915/report.json`.
UTC 2026-09-14 21:15:54–58, Moscow 2026-09-15 00:15:54–58 (request timestamps).
One connection, four operations, no retries, zero owned tasks after shutdown.
Cold connection/handshake1188ms, separate from per-operation timings.

| Operation | Semantic result | Result | User-path ms |
|---|---|---|---:|
| Mixed RU | MIXED / ownship.pitch | Strict schema, fixture Core and isolation pass | 1156 |
| Follow-up RU | FACT_REQUEST / ownship.pitch | Correct referent, fixture Core and isolation pass | 843 |
| Mixed EN | MIXED / ownship.true_airspeed | Strict schema, fixture Core and isolation pass | 1062 |
| Follow-up EN | FACT_REQUEST / ownship.pitch | Wrong older referent; expected ownship.true_airspeed | 797 |

First proven divergence is operation four's semantic selection, not JSON shape,
auth, timeout, Core value production, TTS or SRS. Its interaction ID is
`7c5a982b-5bf6-4b49-9566-9e743a5d8fa4`, operation
`ed4d1ae3-44c8-4abe-9c2b-2d34a203c39f`, provider response
`resp_ce0bcf5dd9964e2286433bbebabb205d`, provider session `0e78b0e20d02`.
RAW, normalized and parsed evidence all select ownship.pitch. Context revision4
contains the older pitch exchange followed by the latest true-airspeed Mixed
exchange, with explicit topic/requested_capabilities. The adapter constructs
instructions from that projection and emits context_applied_ack only after
checking session identity and exact returned instructions equality. Thus this
is not evidence of an omitted context projection or a rejected context update.
It does not prove the provider's internal reason for selecting the older topic;
model attention, prompt interpretation and hidden-state hypotheses are unproven.

The gate's expected-selection assertion stopped BEFORE core.execute for operation
four. That assertion is developer-gate validation, NOT a claim that production
Core detects every semantically wrong but valid catalog selection. Core remains
authoritative for values; that does not certify the AI's referent selection.
No TTS/SRS/audio was used anywhere in this session. Both Mixed schemas passed,
but complete integration gate FAIL (`unexpected_fact_selection`). No field PASS.
Generated prose quality is also not certified by structural/selection checks.
These checks do not replace the required user blind content/acoustic acceptance.

STOP before candidate commit/build/install. No further provider session authorized.
No runtime changes after this second gate; preserve both reports and worktree.

## K–O. Candidate/build/install/readiness/physical boundary

No Step 5 commit/push, build or install. Pre-field candidate decision: **C. BLOCKING
DEFECT**, live follow-up referent selection failed after the schema correction.
Installed artifact remains
unchanged; no current Step 5 installed identity is claimed. Read-only process
inspection found no running ORION/DCS/SRS processes. No processes were started.
No physical test requested or performed, no READY claim.

After one reviewed installed candidate is actually ready, provide only broad
families: free conversation/knowledge, conversational and factual follow-ups,
identity/navigation/another exposed family, alternate wording, multiple facts,
Mixed, META and truthful unavailable, with RU/EN where practical. Never reveal
developer gate sentences or coach blind wording. Maximum primary blind session
plus one focused retest under the current authorization.

Foundation remains NOT FIELD PASSED; prior product FIELD FAIL is preserved.
No domain/Planner/persistent context/phraseology expansion. User data and all
uncommitted Step 5 work are preserved; no data or external evidence is staged.
